name: release

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: memory-linux-x86_64
            bin_path: target/release/memory
            archive_name: memory-linux-x86_64.tar.gz
          - os: macos-latest
            artifact_name: memory-macos-x86_64
            bin_path: target/release/memory
            archive_name: memory-macos-x86_64.tar.gz
          - os: windows-latest
            artifact_name: memory-windows-x86_64
            bin_path: target/release/memory.exe
            archive_name: memory-windows-x86_64.zip
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - name: Build release binary
        run: cargo build --release -p memory-cli
      - name: Package (Unix)
        if: runner.os != 'Windows'
        shell: bash
        run: |
          mkdir -p dist
          cp "${{ matrix.bin_path }}" dist/memory
          tar -czf "${{ matrix.archive_name }}" -C dist memory
          shasum -a 256 "${{ matrix.archive_name }}" > "${{ matrix.archive_name }}.sha256"
      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist | Out-Null
          Copy-Item "${{ matrix.bin_path }}" dist\memory.exe
          Compress-Archive -Path dist\memory.exe -DestinationPath "${{ matrix.archive_name }}" -Force
          $hash = (Get-FileHash "${{ matrix.archive_name }}" -Algorithm SHA256).Hash.ToLower()
          "$hash  ${{ matrix.archive_name }}" | Set-Content "${{ matrix.archive_name }}.sha256"
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact_name }}
          path: |
            ${{ matrix.archive_name }}
            ${{ matrix.archive_name }}.sha256

  publish:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: release-artifacts
      - name: Combine checksums
        shell: bash
        run: |
          find release-artifacts -name '*.sha256' -print0 | sort -z | xargs -0 cat > checksums.txt
      - uses: softprops/action-gh-release@v2
        with:
          files: |
            release-artifacts/**/**
            checksums.txt
          generate_release_notes: true
