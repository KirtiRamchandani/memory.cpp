# Adoption checklist

`memory adoption` is local-only activation diagnostics. It does not send telemetry.

```bash
memory adoption status
memory adoption checklist
memory adoption score --json
memory adoption fix
```

It checks setup, Git watch, terminal memory, context packs, maps, inbox review, privacy docs, attach config, and memory availability.
