# memory doctor

memory doctor is the first command to run when something feels wrong.

```bash
memory doctor
```

It checks database health, workspace status, Git status, Cursor and Claude config, MCP safety defaults, redaction, Ollama reachability, runtime files, API/proxy ports, Git watch baseline, and smoke-test scripts.

It prints exact fix commands when possible.
