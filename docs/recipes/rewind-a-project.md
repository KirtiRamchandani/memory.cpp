# Rewind a project

Goal: see what changed recently.

```bash
memory timeline week
memory rewind last-week
memory changed --since "7 days ago"
```

Expected output: chronological memory and Git notes plus next actions.

What happened: local memory events and Git commits were combined into a repo time machine.

Privacy note: nothing leaves the machine.
