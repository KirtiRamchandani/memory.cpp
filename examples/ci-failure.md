# Example: memory ci explain-failure

```text
CI failure explanation:
- cargo fmt failed because Rust files had CRLF line endings.
previous fix: add .gitattributes and normalize source files to LF.
```
