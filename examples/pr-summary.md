# Example: memory dev pr-summary

```text
$ memory dev pr-summary
Summary: Developer adoption polish for install, attach, watch, context, CI, and docs.
Risk: CLI parser surface grew; run parser tests and smoke scripts.
Tests: cargo test, scripts/smoke.ps1
```