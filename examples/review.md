# Example: memory dev review

```text
$ memory dev review
Review focus:
- Attach writes backups before config changes.
- MCP remains read-only by default.
- Install fallback does not require sudo.
```