# Twitter/X thread draft

1. Your repo remembers.
2. `memory dev morning` tells you what changed, what broke, and what to do next.
3. `memory context write --for cursor` gives AI coding tools grounded local context.
4. `memory map --type evolution --output html` creates a project time machine.
5. Local-first: SQLite under `.memory.cpp/`, read-only MCP by default, terminal memory opt-in.
